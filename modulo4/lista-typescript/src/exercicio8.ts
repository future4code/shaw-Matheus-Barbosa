function renovaIdentidade (nascimento : string , vencIdentidade : string){

    let nasc : string[]
    let ident : string[]
    let data : string[]

    nasc = nascimento.split("/")
    ident = vencIdentidade.split("/")

    const dataAtual = "01/06/2022"
    data = dataAtual.split("/")


}